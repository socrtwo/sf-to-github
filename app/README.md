# sf-to-github — App

React + TypeScript + Vite web app. See the [root README](../README.md) for full documentation.

## Quick start

```bash
npm install
npm run dev        # web dev server  (http://localhost:5173)
npm run electron:dev  # desktop dev
npm test           # unit tests
npm run build      # production web build → dist/
```
